const CyberFooter = ({ profileName }) => (
  <footer className="font-cyber-mono text-[9px] text-slate-700 text-center py-4 tracking-widest uppercase">
    // {profileName || "PORTFOLIO"} · PORTFOLIO STUDIO · {new Date().getFullYear()} //
  </footer>
);

export default CyberFooter;
